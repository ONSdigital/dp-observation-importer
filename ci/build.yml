---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: 1.23.2-bullseye

inputs:
  - name: dp-observation-importer

outputs:
  - name: build

run:
  path: dp-observation-importer/ci/scripts/build.sh